export type CustomerRegister = {
    image: string;
    email: string;
    password: string;
    fullName: string;
    phone: string;
    gender: string;
    dateOfBirth: string; // ISO date string
};